# 14 — Iteration Template (paste into Codex)

```
Plan first (print file changes & diffs), wait for OK.
Then edit, build (swift build -c release). If errors, quote and fix (≤3 cycles).
Run app; export site. Update QA & provenance; stage and call scripts/smart_pr.sh.

Constraints:
- Edit only under workspace/
- Preserve wording; no paraphrasing
- Stable anchors; ToC collapsible
- Add 📎 links from readable → facsimile
```
